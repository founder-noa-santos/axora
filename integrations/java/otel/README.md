# com.axora:logger-otel

OpenTelemetry adapter boundary for AXORA Wide Events.

## Notes

This package accepts a minimal logger-provider bridge so the mapping stays
vendor-agnostic at the SDK boundary.
